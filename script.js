new Chartist.Line('.ct-chart', {
    labels: [],
    series: []
  }, {
    fullWidth: true,
    chartPadding: {
      right: 40
    }
  });

const fRunge = (array,n,e,c,a,b) => {
  const h = (b -a) / n;

  for(let i = 0; i < )

};